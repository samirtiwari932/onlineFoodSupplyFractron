{
    "name": "online-food-supply-fractron",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && rm -rf public && mv client/dist public",
        "server": "npm start --prefix server",
        "client": "npm run dev --prefix client"
    }
}